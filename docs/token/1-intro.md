# 密钥分组查看


* * *

## 如何查看最新分组

登录控制台后，进入左侧"API 密钥"页面，点击"创建密钥"，在弹窗的"分组"下拉菜单中可以查看当前所有可用分组。

每个分组对应不同的模型和计费倍率：

  * 倍率 < 1 享折扣：0.8倍 = 8折，0.5倍 = 5折
  * 倍率 > 1 需加价：1.5倍 = 额外支付50%费用
  * 不选择则使用默认计费倍率（1x）

如需了解各分组详情，请参考 [分组介绍](/token/2-group)。
